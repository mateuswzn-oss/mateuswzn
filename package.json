{
  "name": "klyne",
  "private": true,
  "version": "1.0.0",
  "description": "Klyne — site estático com um endpoint serverless (/api/ai) para a Nyc AI.",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.121.0"
  },
  "scripts": {
    "teste": "tools/testes/rodar.sh"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
